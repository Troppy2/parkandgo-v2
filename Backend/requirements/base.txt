#Top-level Notes: use pip install -r requirements/base.txt to install all dependencies
# Framework
fastapi==0.109.0

# Database & ORM (PostgreSQL + PostGIS)
sqlalchemy==2.0.23
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
GeoAlchemy2==0.14.3

# Config & validation
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# HTTP client (async + sync for services/geocoding)
httpx==0.26.0
requests==2.31.0

# Production ASGI server
gunicorn==21.2.0

# Auth: JWT, password hashing, OAuth2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
oauthlib==3.2.2
cryptography==41.0.7

# Caching (Redis) – core/caching.py
redis

# Task queue (Celery) – tasks/worker.py
celery[redis]==5.3.6

# Observability – utils/metrics.py
prometheus-client==0.19.0

# Database migrations
alembic
psycopg2-binary

# Campus Event Ical dependency
icalendar
apscheduler

# Rate limiting
slowapi
