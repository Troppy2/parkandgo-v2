<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production traffic goes to https://parkandgo-v2.onrender.com over TLS,
        so cleartext is disabled by default. Cleartext is permitted only for
        local development hosts (Android emulator loopback and localhost) so a
        debug build can be pointed at a dev backend when needed.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
