# Loaded automatically by `vite build` (production mode).
# Used when building the web bundle that gets packaged into the Android APK.
# Local dev keeps using .env (localhost). Do not point this at localhost,
# a phone cannot reach your laptop's localhost.
VITE_API_URL=https://parkandgo-v2.onrender.com/api
